# Default team to use if not specified in CSV or if mapping fails
default_team: "Spuyt"

# Default channels to add every user to (per team context)
default_channels:
  - "Town Square"
  - "Off-Topic"

# Settings
enable_wet_run: false  # Safety: requires --execute flag in CLI to actually change state
